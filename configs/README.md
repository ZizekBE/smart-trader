# configs

- **生产运行角色、RL OOS 冻结参数、迭代进度**：见仓库 [`docs/ROADMAP.md`](../docs/ROADMAP.md) 章节 **「迭代进度与冻结约定」**（与 [`docs/implementation_todos.md`](../docs/implementation_todos.md) 中 `EPIC-OOS` / `EPIC-RL` 同步）。
- **非敏感环境变量示例**：[`envs/.env.example`](envs/.env.example)。密钥仅 `secrets/.env`（gitignore，勿提交；与 `configs/envs` 并列于本目录，见仓库 `.cursor` 约定）。
